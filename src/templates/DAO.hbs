import { AbstractModel, Model } from 'alliance/db';
import { {{name}}Table } from '../tables/{{nameCamelCase}}.table';

@DAO({{name}}Table)
export class {{name}}Model extends AbstractDAO<{{name}}Table> {

    // {{name}} DAO

}
